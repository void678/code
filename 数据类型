1.shift + alt + 向下箭头   复制粘贴上一行

2.在程序中查看某个变量的占用的字节大小和数据类型
   fmt.Printf("n的类型是 %T n占用的字节数是 %d", n1 ,unsafe.Sizeof(n))
   unsafe.Sizeof()是unsafe包中的一个函数，可以返回变量占用的字节数
   需要引入unsafe包 import "unsafe"

3.引多个包
  import(
      "fmt"
      "unsafe"
  )

4.
